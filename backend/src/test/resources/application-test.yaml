---
spring:
  flyway:
    placeholders:
      app_user: app_user
      app_password: app_password
    user: postgres
    password: postgres
  datasource:
    username: app_user
    password: app_password
app:
  url: http://localhost:3000
  rp-id: localhost
